name: Legacy MCU Tracker App Publisher (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled_notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy publisher disabled. App content is managed by the current release flow."
